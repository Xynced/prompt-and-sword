# Prompt & Sword — фазы 0–5

Роуглайк-автобатлер, где билд — это текст. Полная проработка: [prompt_and_sword_plan.md](prompt_and_sword_plan.md).

## Запуск

```bash
pnpm install
pnpm test          # юнит-тесты (включая критерии Ворот A)
pnpm sim gateA     # статистика: 5 наборов IR × 50 сидов + тест дивергенции
pnpm sim run rush 1  # подробный лог одного боя (набор, seed)
pnpm sim demo-run 1  # забег целиком по карте: карточки, бои, события, босс
pnpm sim balance 1000  # автобаланс: бот играет N забегов, кривая сложности по слоям
pnpm dev             # веб-UI: играбельный забег (http://localhost:5173, ?seed=N или ?build=ps1.…)
pnpm build           # статическая сборка в dist/ (демо для itch.io)

# Фаза 3 — LLM-компилятор (нужен ключ; без него всё работает через конструктор):
pnpm sim compile "Прикрывай Лию, а если врагов больше — отступай"  # текст → IR → карточка
pnpm sim corpus      # прогон corpus/*.txt: % скомпилированных / с неуверенностью / дифф с намерением
```

Провайдер компилятора настраивается через env (по умолчанию Anthropic / `claude-sonnet-5`):

```bash
# Отладка на DeepSeek (Anthropic-совместимый эндпоинт):
export COMPILER_API_KEY=sk-...            # ключ DeepSeek
export COMPILER_BASE_URL=https://api.deepseek.com/anthropic
export COMPILER_MODEL=deepseek-chat
# Финал: убрать эти переменные и задать ANTHROPIC_API_KEY — вернётся claude-sonnet-5.
```

Подойдёт любой Anthropic-совместимый эндпоинт (DeepSeek, LiteLLM-шлюз и т.п.).
Идентификатор модели входит в ключ кэша — смена провайдера не отдаёт чужие
закэшированные компиляции. Валидация выхода (схема из открытого словаря +
программная проверка + гейт `compilePhrase`) не зависит от провайдера: слабая
модель может ошибаться чаще, но невалидное правило не выдаст. Целевые ≥85%
Ворот C меряются на финальной модели.

Наборы для `run`: `rush`, `focus-leader`, `turtle`, `guard-mage`, `kite`, `rush-variant`.

## Что реализовано

- **IR** ([src/ir.ts](src/ir.ts), [src/vocab.ts](src/vocab.ts)) — 44 концепта: 6 стартовых (атаковать, ближайший, слабейший, HP ниже X, отступать, держать позицию), 13 базовых (врагов больше, союзник в опасности, «враги накатывают», вожак, защищать, рядом с, позади, манера удара «бить часто»/«бить наверняка», слова поля «держать высоту», «за укрытием», «обходить опасное», «держать интервал») и 24 глубоких — «бой затянулся», «меня окружили», помеченный, самый опасный, стрелок, **приманка**, **размен**, **держать дистанцию**, **прикрывать отход**, **толкать**, **накрыть скопление**, **бить на упреждение**, **замахиваться ритуалом**, **впасть в ярость**, **лечить**, фланг, узкое место, вне линии огня и т.д. — стратегии, невыразимые ранним словарём. Условные правила гейтят и касты, и классовые активы: «если враги накатывают — замахиваться ритуалом», «если врагов больше — впасть в ярость» — работающие фразы. Всё вне старта открывается трофеями боёв, в скриптории и у книжника.
- **Линзы характеров** ([src/lens.ts](src/lens.ts)) — детерминированные трансформации IR, 10 в пуле: трус (прикрывать → стоять позади, штраф рискованным правилам, бегство при hp<30%), фанатик (отступление → атака, игнор ZoC), буквалист (нет правила → глухая оборона), мститель (бьёт обидчика), дуэлянт (слабых не добивает, в спину не бьёт), славолюб (цель — только вожак), наседка (усиленная защита своих, тянется к «стене»), параноик (вне линии огня), горячка (не стоит на месте), позёр (фланги и приманка). Линзы искажают и классовые активы: фанатик срывает условие с ярости («не ждёт повода»), трус в неё не впадает вовсе (уходит в глухую оборону). Каждый герой при генерации забега получает 1–3 случайные линзы (детерминировано сидом); линзы применяются по порядку, их искажения складываются.
- **Боевое ядро** ([src/battle.ts](src/battle.ts), [src/grid.ts](src/grid.ts)) — поле 18×18 из клеток со свойствами ([src/terrain.ts](src/terrain.ts): камень, высота, опасность, бурелом; 12 арен-сценариев ASCII-схемами, пул по тегу узла, выбор от сида), инициатива по скорости, фланг/LoS/ZoC, seeded RNG (mulberry32), event-sourced лог. Ландшафт играет: стрелку с холма +дальность/+урон, камень у цели режет выстрел вдвое (высота 2 бьёт поверх), шипы наказывают неосторожный шаг (действия «осторожный шаг» и «толкать» — в словаре), бурелом и подъём стоят вдвое. Тяга к цели идёт по взвешенному полю дистанций — юниты огибают стены и болота и стягиваются в проходы. Перед боем — расстановка партии в зоне развёртывания; разведка показывает арену и (у элиток и босса) расстановку врагов. **Площадные атаки** (план АОЕ): кучковаться дорого — залп 3×3 и волна 1×4 бьют сразу, ритуал 5×5 телеграфируется на ход вперёд (из зоны выходят, прикрываются или убивают кастера — смерть отменяет замах); friendly fire включён для всех, урон фиксированный без rng. Носители: шаман, вождь орды, Лия (залп + ритуал раз в бой), Жало-«спеллблейд» (волна); у героев касты гейтятся словом «накрыть скопление», «бить на упреждение» целит ритуал в проекцию движения. **Классы** (план классов): пул из 16 героев — 8 классов pf2e по два варианта; урон и дальность живут на именованном оружии (`WeaponSpec`; мастер Яр носит три и выбирает по ситуации), аффинность манер мягкая — слово игрока перебивает. Классовые активы гейтятся словами и правилами: ярость Ульва, стена Грома, исцеление Ивы (первый «hp вверх», слово «лечить»), благословение Радима, финт Лисы; «полымя» Весты — ритуал, зона которого жжёт три хода подряд; «росчерк» Рыка — короткая волна двуручника. Пассивы включены всегда: щит −40%, метка ударом, незыблемость, тень, «в спину», кара Зари, кулаки Юны. Всё видно в карточке героя и разведке врага.
- **Utility-скоринг** ([src/scoring.ts](src/scoring.ts)) — инстинкты (класс/характер) + веса сработавших правил; топ-3 фактора каждого решения пишутся в лог.
- **Конструктор принципов** ([src/constructor.ts](src/constructor.ts), [src/vocab.ts](src/vocab.ts)) — фразы из чипсов словаря → IR без LLM; закрытый концепт — ошибка компиляции, не догадка.
- **Карточки «как понял»** ([src/cards.ts](src/cards.ts)) — шаблонный обратный перевод IR после линзы; искажения помечены ⚠ и видны до боя.
- **Спарринг** ([src/sparring.ts](src/sparring.ts)) — тот же seed, переписанные принципы; дифф: исход, раунды, потери, раунд расхождения.
- **Забег по карте** ([src/run.ts](src/run.ts)) — ветвящаяся карта из 15 узлов (à la Slay the Spire): урок / бои / элитки / события / привал / скрипторий / босс; игрок выбирает путь. HP переносится между боями (перевязка +25% после победы, привал в середине лечит 60% недостающего), пермасмерть. Перед боссом — «канун битвы»: живые входят в бой с полным hp (босс — задача на контр-формулировку, а не проверка запаса hp; погибших канун не воскрешает). События: книжник (концепт задаром), тайник (+1 слот), наёмник — занимает место погибшего с **другим набором линз**, прежние принципы читаются иначе. Всё детерминировано сидом забега.
- **Онбординг** — первый узел «урок»: по симу (100 сидов, случайные линзы) дефолтные принципы проигрывают ~60% сидов, универсальный кайт выигрывает ~52% — выигрышная формулировка зависит от выпавших линз, урок учит читать карточки «как понял». Поражение в уроке ничего не стоит (забег не кончается, раны не переносятся) — UI сам предлагает переписать приказ и переиграть с теми же костями.
- **Враги и паттерны боёв** ([src/foes.ts](src/foes.ts), план врагов) — 19 типов: к семи из ранних фаз (рубака, лучник, вожак, вождь, шаман, берсерк, охотник) добавлены 12 по мотивам pf2e, собранные в **паттерны** — составы, ломающие прошлую выигрышную формулировку: **масса** (9 крыс-фанатиков — тел больше, чем ходов; контр — «накрыть скопление»), **стая** (волки-охотники: фланговый укус ×2, режут хрупких; контр — фокус-огонь), **застрельщики** (кобольды-пращники с пращой и ножом — камнем по раненым), **умная элита** (латники держат строй, сержант кричит «клич мести», когда падает латник, — порядок целей решает), **ближники + лекарь** (гноллы-налётчики с луком и топором, костоправ перелечивает — мета добивания вязнет), **танк + кастеры** (огр-пробка, поджигатель жжёт из-за спины; контр — «держать интервал»), **засада** (бугбер-душегуб охотится на тыл), **таймер** (тролль зарастает +8 в ход — распылять урон нельзя), **дуэль** (орк-поединщик с линзой дуэлянта выпиливает самого ударного героя; вызов принимают всем строем). Вождь орды — прежний босс. Новых слов паттерны не требуют — только новые сочетания старых; вся машинерия (оружие 1–3, активы heal/bless, пассивы, линзы) — переиспользование плана классов, единственное новое — пассив регенерации. У урока, элиток и босса перед боем видна **разведка** — принципы врагов после линзы (`foeIntel`), оружие, активы и пассивы: бой — задача на контр-формулировку.
- **Автобаланс прогонами** ([src/balance.ts](src/balance.ts), фаза 5) — детерминированный бот играет забег целиком (дефолт → кайт после поражения в уроке, путь случайный от сида); `pnpm sim balance 1000` — 1000 забегов за ~2 секунды: winrate по слоям и типам узлов, истощение hp, роковые бои, смерти героев. Числа тюнинга подбирались этим симом (см. «Фаза 5 — баланс»).
- **Экспорт билда строкой** ([src/share.ts](src/share.ts), фаза 5) — «вот мой билд, побей мой сид»: `ps1.<base64url>` = сид + открытый словарь + слоты и принципы. Кнопка «экспорт билда» на карте и в конце забега кладёт в буфер ссылку `?build=…`; импорт строит свежий забег с того же сида (та же карта, те же линзы) и гоняет строку через ту же валидацию, что у конструктора — чужая строка не выразит закрытый концепт. Семя будущего PvP.
- **LLM-компилятор** ([src/compiler/](src/compiler)) — свободный текст → те же `PhraseDraft`/IR, что и у конструктора (strict tool use; модель настраивается, по умолчанию `claude-sonnet-5`). Строгая JSON-схема строится из **открытого** словаря — закрытый концепт невыразим на уровне схемы; поверх — программная валидация и финальный гейт тем же `compilePhrase`. Неоднозначность → простейший маппинг + заметка в `uncertainty` (⚠ на карточке до боя). Кэш по полному ключу текст+словарь+характер: `.cache/compiler.json` (CLI) / localStorage (UI). LLM в бою не участвует; тесты компилятора — на моках.

Инвариант: тот же seed + те же принципы = тот же бой (тест на побайтовое совпадение лога).

## Ворота A — статус

Прогон `pnpm sim gateA` (50 сидов):

| Критерий | Результат |
|---|---|
| Разные наборы правил → статистически разные исходы | winrate 12–82%, длительность 13.2–15.6 раундов, centroid.x 1.02–3.15 |
| Смена 1 правила меняет ход боя в ≥60% сидов | **100%** (исход переворачивается в 36%) |
| Лог решений читается | `pnpm sim run <set> <seed>` — у каждого решения топ-3 фактора с именами правил |

Критерии закодированы в [test/gate-a.test.ts](test/gate-a.test.ts).

## Веб-UI (фазы 2 и 4)

[src/ui/main.ts](src/ui/main.ts) — чистый DOM + Vite, без фреймворка, стилистика
«полевой дневник тактика» (пергамент, чернила, сургуч). Экран карты (SVG-граф
узлов, клик по достижимому узлу — переход), панель узла (бой с разведкой /
скрипторий / событие / привал), конструктор фраз с живыми карточками «как понял»,
полосы hp героев между боями, мини-поле расстановки с превью арены. Экран боя:
поле 18×18 со свойствами клеток (камни, высоты, шипы, огонь, бурелом),
автопрокрутка, лог решений с факторами, «Переиграть с теми же костями».

Свободный текст в UI: задай в `.env` ключ `VITE_COMPILER_API_KEY` (совместимо и
`VITE_ANTHROPIC_API_KEY`), опционально `VITE_COMPILER_MODEL` и
`VITE_COMPILER_BASE_URL` (напр. DeepSeek для отладки) — у каждого героя появится
тоггл «✎ текстом» рядом с конструктором. Без ключа UI работает только на
конструкторе (оффлайн-режим). Ключ уходит в браузер — только для локальной
разработки.

## Ворота B — что осталось

Игра играбельна end-to-end. Осталось: 10 своих забегов + 3 внешних тестера,
наблюдение за спаррингом и считываемостью характеров (см. критерии в плане).

Для плейтестов в UI встроен **журнал плейтеста** ([src/playtest.ts](src/playtest.ts)):
редактор приказов просит написать замысел словами до сборки из чипсов (по плану —
так корпус Ворот C собирается ещё на Воротах B), а поведение тестера пишется в
localStorage: бои, добровольные спарринги, переписывания приказов между попытками
(критерий «петля работает»). Кнопка «журнал плейтеста» (футер карты и экран конца
забега) копирует отчёт в буфер — тестер присылает его одним сообщением.

## Ворота C — подготовка

Корпус формулировок — [corpus/sample.txt](corpus/sample.txt) (формат: `текст`
или `текст => ожидаемое понимание`); реальные 100 формулировок собираются с
плейтестеров на Воротах B — секция «корпус формулировок» из журнала плейтеста
вставляется в `corpus/<тестер>.txt` как есть (контекст героя и линз — в
`#`-комментариях, парсер их пропускает). `pnpm sim corpus` печатает отчёт:
% скомпилированных, % с неуверенностью, совпадение с намерением. Критерий «инъекции отбиваются
схемой» закодирован в [test/compiler.test.ts](test/compiler.test.ts).

## Фаза 5 — баланс

Автобаланс прогонами (`pnpm sim balance`) нашёл и снял две стены:

- **Босс был нерешаем**: 0/380 у бота; лучший из 8 контр-билдов выигрывал 35%
  только на полном hp и 0% при ≤80%, а партия приходила в среднем с 43%.
  Правки: вождь atk 9→8 (на полном hp наив «фокусь вожака» ~22%, контр-билды
  ~60%, решаемость портфелем билдов 88%) и «канун битвы» — к боссу партия
  входит с полным hp. Ослаблять босса сильнее нельзя: уже при atk 8 и вожде
  46hp наив почти догоняет контр-билды — исчезает «формулировка решает».
- **Кривая была плоской с обрывом**: слои 1–3 выигрывались в 98–100%, все
  смерти сидели на слое 5. Привал переехал с предбоссового слоя в середину
  (лечит перед смертельным слоем 5), на его месте — событие (шанс нанять
  наёмника на финал; привал перед кануном был бы мёртвым выбором).

После правок: бот (нижняя граница игрока — принципы не переписывает) выигрывает
10% забегов, смерти делятся между слоями 5 и 7 (52%/47%), босс у статичного
бота — 27%. Урок решаем в 100% из 300 сидов стартовым словарём (перебор до 216
билдов на сид); две заготовки бота пробивают 66% — остальное и есть место
игрока. Ключевое наблюдение фазы 4 в силе: 4-й враг против партии из трёх
делает бой почти нерешаемым (экономика действий).

## Демо на itch.io

```bash
pnpm build   # статическая сборка в dist/ (относительные пути — base './')
cd dist && zip -r ../prompt-and-sword-demo.zip . && cd ..
```

Загрузка: itch.io → New project → Kind of project: **HTML** → загрузить zip,
отметить «This file will be played in the browser». Размер окна — 960×640
(книга сама масштабируется под меньшее). Содержимое демо — по плану: 1 забег,
3 героя, 30 концептов.

Важно: не собирать публичную демку с `VITE_COMPILER_API_KEY` в `.env` — ключ
попадёт в бандл. Без ключа демо работает в оффлайн-режиме на конструкторе,
что и нужно для паблика.

## Демо на GitHub Pages

[.github/workflows/deploy.yml](.github/workflows/deploy.yml): пуш в `main` →
тесты → сборка → публикация на `https://<юзер>.github.io/<репо>/`. Разовая
настройка в репозитории: Settings → Pages → Source: **GitHub Actions**.

Свободный текст: секрет `COMPILER_API_KEY` (Settings → Secrets and variables →
Actions) вшивается в сборку; эндпоинт и модель (DeepSeek) заданы в workflow.
**Ключ в статической сборке публичен** — любой посетитель может его извлечь из
JS-бандла и тратить баланс. Осознанный компромисс для приватной демки: держать
на ключе копеечный баланс. Без секрета страница собирается в оффлайн-режиме.

## Сознательно отложено (по плану)

- Рывок-действие из исходного плана — по-прежнему отложен (остальные активки
  закрыл план классов: лечение, ярость, стена, метка, финт, благословение).
- Слова для благословения и финта — намеренно не введены (жмутся врождёнными
  правилами носителей); давать ли их игроку — решит план `words`.
- «Как понял» голосом персонажа (LLM-обратный перевод) — сейчас шаблоны + заметки
  неуверенности; флейвор после Ворот C.
- Party-scope правила («вы двое — фокусите») — не в MVP. Командный фокус-огонь
  реализован без них: метка ставится на врага на уровне забега
  ([src/run.ts](src/run.ts)), селектор «помеченный» читает её у каждого героя.
