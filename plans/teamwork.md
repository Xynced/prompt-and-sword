# План: Бой — внутрикомандное взаимодействие (внимание и цена дороги)

Статус: в работе · команда: `continue teamwork plan`

## Проблематика

Три связанных дыры:

1. **Выбор цели — скрипт, а не решение.** Правило «атаковать X» разрешает
   селектор в одну цель и тянет к ней линейно (−0.6 × gap), не зная цены
   дороги: волк с «резать слабых» ныряет за Лией сквозь строй и
   телохранителя, хотя удар перехватят, — а рядом стоит доступный боец.
   Обратного решения тоже нет: выбор «ударить сейчас или рискнуть и
   додавить слабого в тылу» движку недоступен — «синицу в руке» видит только
   слабый инстинкт агрессии на конечной клетке. Следствие для обороны (урок
   плана defense): перехват наказывает нырок постфактум, но враг не умеет
   учитывать его заранее — защита штрафует, а не сдерживает.

2. **Канала внимания нет — отвлечение невыразимо.** «Отвлекай врагов от Леи,
   старайся уводить их в сторону» не пишется никакими словами: ни одно
   действие не меняет привлекательность целей для противника. Приманка
   (bait) делает носителя живучим, но не даёт врагу причин идти именно к
   нему.

3. **Инвентаризация «что уже есть»** (чтобы не дублировать): следование —
   `space.nearTo` / `space.behind` с якорем-союзником (двусторонняя
   дисциплина — урок плана defense); фокус — `act.focusFire`, `sel.marked`,
   `act.finish`, `sel.tormentor`; защита — перехват, щит, стена, строй,
   рипост. Этим план не занимается — только вниманием и глубиной выбора
   цели.

Смежные планы: `words` (новое слово, редкость, перемер аудита), `lenses`
(восприимчивость к провокации и риск-аппетит — характерные множители),
`foes` (риск-аппетит по архетипам, возможный новый враг), `defense` (заслон
должен начать сдерживать, а не только наказывать).

## Проработка

_Согласована с пользователем 2026-08-14: цена дороги — симметрично обеим
сторонам (перемер words-audit обязателен); канал внимания принят; слова —
два отдельных: «вызывать на себя» (стойка) и «уводить от X» (движение)._

Общий принцип: **никакого mind control** — внимание и цена дороги входят
слагаемыми в тот же utility-скоринг, симметрично для обеих сторон; слова
только гейтят и взвешивают, характеры искажают множителями.

### 1. Цена дороги: доступность цели (правило мира, обе стороны)

Тяга правила «атаковать X» (и премия шага под удар) умножается на
**доступность цели** `appeal ∈ [APPEAL_FLOOR..1]`:

- **заслон**: у цели смежный живой страж со сработавшим `protect` (перехват
  заряжен) → ×0.45;
- **дорога**: путь до дальности удара длиннее, чем до ближайшей
  альтернативной цели, — 0.08 за клетку сверх трёх бесплатных;
- скидки за глухую оборону цели **нет**: пробовалась (0.7) и убрана по
  замеру — «умный переключается с черепахи» уже выражено штрафом «рипост цели»
  из плана defense, и дубль лишь портил оборонительное слово (см. журнал);
- **осторожность** — новый инстинкт `caution`: множитель на видимую скидку.
  Фанатик и буквалист 0 — цены дороги не видят и прут за целью приказа; трус
  1.4 и параноик 1.3 преувеличивают; дуэлянт 0.3 не бросает вызванного,
  горячка 0.4 ведётся.

**Ключевой урок реализации (замер, 2026-08-15).** Ослабления тяги мало:
инстинкт агрессии по доступной цели даёт ≈0.5 очка против премии правила
3 × вес = 6, поэтому боец с ослабленным приказом не выбирал «синицу в руке»,
а бесцельно обходил строй по дуге вне зон контроля (0 ударов по доступному
танку на 20 боях). Поэтому правило **переезжает на доступную цель целиком**:
приоритет цели приказа — 1, любой другой — ровно недоступность приказанной,
и весь градиент правила (тяга, премия удара, премия шага под выстрел)
считается по выигравшей. Порог подмены — «цель приказа дороже доступной»
(appeal < 0.5), поэтому заслон обязан переваливать его сам: отсюда 0.45,
а не 0.6.

При доступной цели (appeal = 1) приоритет прочих нулевой — правило читается
буквально, как до плана, и существующие замеры не плывут. Юнит без
альтернатив не застывает: у единственной цели крюка нет, а пол скидки
оставляет приказ рабочим.

### 2. Канал внимания (провокация)

Стойка `taunt` по образцу bait/манер: держится до следующего решения носителя
(её ставят оба слова — и «вызывать на себя», и «уводить от X», чтобы связка не
платила за вызов дважды). Пока провокатор жив и досягаем для врага
(strikeReach), доступность **всех остальных** целей этого врага множится на
(1 − TAUNT_PULL × provocable); сам провокатор скидки не платит — потому
подмена §1 и переезжает на него, отдельного канала выбора цели нет.

Реализовано **второй, независимой осью** доступности, а не ещё одной скидкой
цены дороги: фанатик дорогу не считает (`caution` 0), но на дерзкий выкрик
оборачивается — это разные черты, и слепота к одной не даёт слепоты к другой.
Восприимчивость `provocable`: горячка 2, дуэлянт 0.3, буквалист 0 («ему
сказано, кого бить»).

TAUNT_PULL = 0.55 — минимум, при котором приказ действительно переезжает на
крикуна (порог подмены 0.5); ниже слово лишь слегка мяло бы выбор, и игрок не
увидел бы отвлечения вовсе.

Механика симметрична: тем же приёмом враг уводит удары партии на себя (смоук
в тестах), а линза горячки делает героя уязвимым к провокации.

### 3. Два слова: «вызывать на себя» и «уводить от X»

Решение пользователя: не одно составное слово, а пара — гибче связки, приём
собирается из частей.

- **`act.taunt` «вызывать на себя»** (стойка, редкое): включает внимание
  (§2). Само по себе — танковка: связки с «глухой обороной», приманкой,
  строем.
- **`act.lure` «уводить от X»** (движение, обычное; якорь-союзник как у
  `protect`): премия клеткам, досягаемым для врагов (быть у них на виду), плюс
  премия за каждую клетку между собой и X — до предела LURE_SPREAD = 5.
  Кап по уроку слова «подальше от»: без него увод стал бы бегством к краю
  карты, а уводящий полезен ровно тем, что враги идут за ним. Досягаемость
  нулевая → премии нет вовсе: уводить некого.

Связки: «вызывать на себя» + «уводить от Леи» — фраза пользователя
(«отвлекай врагов от Леи, уводи их в сторону»); + «приманка» — загонный
танк; «если Лея в опасности — уводить от Леи» — условная версия;
«вызывать на себя» + «держаться подальше от (стрелок)» — увод в мёртвую
зону.

Отдельное слово «следовать за X» не заводим: следование уже покрыто
«рядом с X» / «позади X», а односторонняя нянька — известная ловушка
(журнал defense).

### 4. Враги и линзы играют новым

- **Спеки врагов править не понадобилось**: риск-аппетит уже выражен линзами,
  которые у них стоят. Крысы и тролль — фанатики (`caution` 0): прут за целью
  приказа, загонная охота за слабыми осталась страшной. Волки, душегуб,
  латники, пращник — `plain` (1): их заслон реально сдерживает.
  Поединщик — дуэлянт (0.3): вызванного не бросает. Разнообразие
  получилось само, без единой правки `foes.ts`.
- Враг-«задира» (гоблин-крикун), который отвлекал бы героев от строя, —
  **не добавлен**: механика уже симметрична (смоук показывает, что любой враг
  со словом вызова уводит удары партии на себя), а новый контент — отдельное
  решение пользователя, не следствие плана.
- Сверка с words-планом: `act.taunt` — в редкие (единственное слово, которое
  меняет ЧУЖОЙ выбор цели), `act.lure` — в обычные; перемер words-audit
  обязателен (цена дороги двигает базу всех слов).

### Риски и замеры

- Цена дороги меняет поведение всех юнитов со словом «атаковать» —
  обязательный перемер words-audit и баланс 500; допуск: средние дельты слов
  не плывут больше ±3пп без объяснения причины.
- Ослабление тяги не должно давать «застывших»: смоук — доля ничьих по
  паттернам не растёт (урок пращника-труса: ничья в забеге = поражение).
- Отвлечение — не кнопка mind control: ATT подобрать так, чтобы враг с явной
  целью (задача killTarget, фокус вожака) на провокацию не вёлся; проверить
  на сценариях objectives.
- Смоуки: «отвлекай от Леи» против стаи волков — выживаемость Леи выше
  наива; нырок за прикрытой целью — латник переключается, берсерк ныряет;
  Ворота A и детерминизм после каждого шага.

### Шаги

1. ✅ **Цена дороги + осторожность**: scoring/lens + тесты + перемеры.
2. ✅ **Канал внимания + стойка**: types/scoring/battle + тесты.
3. ✅ **Слова «вызывать на себя» и «уводить от X»**: vocab/constructor/схема/
   карточки/UI + пулы редкости + смоук связки.
4. ✅ Риск-аппетит врагов (правок не потребовал) + words-audit + баланс 500 +
   Ворота A + README/индексы + журнал.

Остаток (следующая сессия по плану):
5. **Переработать «уводить от X»** — по аудиту слово слабое даже в связке
   (+1пп к вызову). Гипотеза: премия за дистанцию от подопечного слишком
   абстрактна; ниша — «увести туда, где уводящему выгодно» (к узкому месту,
   на высоту, от стрелков) или «увести из зоны, где стоят свои».
6. **Враг-задира** — если пользователь захочет контента: гоблин-крикун
   уводит удары партии от строя, контр — дисциплинированные характеры.

## Журнал

- 2026-08-14 — план создан по запросу: слова внутрикомандного взаимодействия
  (отвлечение, увод, защита со сдерживанием) + глубина выбора цели у врагов
  («атаковать сейчас или рискнуть нырком за слабым»). Проработка согласована:
  симметрия — да, канал внимания — да, слова — два («вызывать на себя» +
  «уводить от X»). Статус «в работе». Следующий шаг: 1 (цена дороги + daring).
- 2026-08-15 — **шаги 1–3 сделаны.** Тесты 522 → 538 (новый файл
  [test/teamwork.test.ts](../test/teamwork.test.ts), 16 сцен), Ворота A PASS,
  детерминизм цел, typecheck чист.
  - **Цена дороги** (шаг 1): `targetAppeal` — заслон ×0.45, глухая оборона
    ×0.7, крюк 0.08/клетка сверх трёх бесплатных, пол 0.3; ось видимости —
    инстинкт `caution` (переименован из задуманного `daring`, чтобы имя
    называло смысл: множитель на видимую цену, а не «смелость»).
    Главная правка проработки — **подмена цели приказа**: одного ослабления
    тяги не хватило (замер: 0 ударов по доступному танку на 20 боях, боец
    просто обходил строй по дуге), потому что агрессия по доступной цели даёт
    ≈0.5 очка против премии правила 6. Теперь при цели дороже доступной весь
    градиент правила переезжает на ту, что под рукой.
    Два калибровочных урока: DETOUR_FREE поднят 1 → 3, потому что при 1
    размывалось «добивай раненых» у партии (теряло 2 боя из 20 на волчьей
    стае); INTERCEPT_APPEAL опущен 0.6 → 0.45, потому что заслон обязан
    переваливать порог подмены сам, без помощи крюка.
  - **Канал внимания** (шаг 2): стойка `taunt` (её ставят оба новых слова),
    TAUNT_PULL 0.55, восприимчивость `provocable` (горячка 2, дуэлянт 0.3,
    буквалист 0). Сделан **второй независимой осью**, а не скидкой цены
    дороги: фанатик дорогу не считает, но на выкрик оборачивается.
  - **Слова** (шаг 3): `act.taunt` (редкое) и `act.lure` (обычное, якорь-
    союзник) — словарь 69 → 71; конструктор, схема компилятора (PROMPT_VERSION
    16 → 17), карточки «как понял», чипсы UI (проверено в браузере: приказ
    компилируется, эхо читается, консоль чиста).
  - **Замеры.** Фраза пользователя «отвлекай врагов от Леи, уводи их в
    сторону» против засады: удары по магу 185 → 31, её смерти 20/20 → 1/20,
    удары переехали на крикуна (284 по Грому). У приёма свой профиль против
    телохранителя (hp партии 0.545 против 0.649 — отвлечение спасает
    конкретного героя, телохранитель экономит суммарное hp): выбор, а не
    доминирование. Старая связка защиты усилилась побочно: удары по магу
    185 → 89, смерти 20 → 4.
    Баланс 500 (сравнение с базовой линией на том же коммите): забег 6 → 7%,
    кривая в пределах ±2пп (босс 41 → 49% на 70 боях — в пределах шума), hp
    после победы на 1–4пп выше. Бот новых слов не знает, поэтому прирост
    достаётся тому, кто пишет слова, — как и в плане defense.
    **Перемер words-audit** (3 сида, 58 записей базы, база против нового кода).
    Промежуточный прогон дал средний сдвиг Δнаив −0.1пп при трёх записях за
    допуском ±3пп: «глухая оборона» −4пп и «накатывают → глухая оборона» −4пп
    (дубль механики, убран — см. ниже) и «самый дальний» +5пп. Итоговый прогон
    на финальном коде: **средний сдвиг Δнаив −0.0пп, Δкайт −0.2пп, максимум
    |4|пп, за допуском одна запись** — «самый дальний» (−25 → −21пп), и это
    приятный побочный эффект: подмена цели смягчает **вредные** приказы, у
    изъятого слова стало меньше зубов. Оборонительные слова вернулись почти на
    место (глухая оборона −34 → −36пп вместо −38пп, остаток внутри допуска).
  - **Смежная правка**: смоук перехвата в defense.test переформулирован.
    Перехватов стало мало (2 против 8) не из-за регресса, а по замыслу: враги
    больше не ныряют за прикрытой целью, и ловить телохранителю нечего —
    критерий заменён на более сильный (удары по подопечной и её смерти).
  - **Оптимизация** (без неё цена дороги удваивала время сима): доступность
    целей мемоизируется на одно решение (`AppealMemo`) — она зависит от клетки
    решающего, а не от кандидата; а крюк читает **одно** поле дистанций от себя
    вместо поля на каждого врага. У тяги правила задача обратная (одна цель,
    много клеток-кандидатов), поэтому там поле по-прежнему строится от цели.
  - **Видимость в разборе боя**: когда приказ переезжает на доступную цель,
    фактор решения так и называется — «правило:вожак: добивать раненых → Дарт
    (кто доступен)». Без этого игрок видел бы, что боец бьёт не того, кого
    велено, и не понимал бы причину; дословно исполненный приказ пометки не
    получает.
  - **Убран дубль механики** (по данным перемера): скидка за глухую оборону
    цели (`TURTLE_APPEAL` 0.7) удалена — «умный переключается с черепахи» уже
    выражено штрафом «рипост цели» из плана defense, и вторая механика на тот
    же случай только портила оборонительное слово (−34 → −38пп наиву), ничего
    не добавляя. Заслон телохранителя ничем не дублируется и остался.

  - **Аудит новых слов** (3 сида, записи в `AUDIT_ENTRIES`) вскрыл ошибку
    дизайна и исправил её. Первый прогон: вызов +6/+8, увод +5/+8, комбо
    +6/+9 — комбо не сильнее слов по отдельности, потому что стойку вызова
    ставили **оба** слова, и «уводить» поглощало «вызывать» целиком (второй
    трофей ничего не добавлял). Стойку вернули только вызову — как и было в
    проработке. После правки числа стали содержательными:
    - `act.taunt` +6пп наиву, +8пп кайту, пик **+25пп** (урок) → **редкое**
      по критериям плана words (есть бой, где решает);
    - `act.lure` сам по себе −4пп наиву, +1пп кайту, пик всего +8пп (огр с
      кастерами), худший −14пп (тролль), а в паре с вызовом добавляет к нему
      всего +1пп → **обычное**, и честно говоря слабое: приём читается и играет
      (смоук связки внизу), но числами почти весь эффект несёт вызов. Слово —
      кандидат на переработку в следующей итерации плана words: ниша «увести
      того, кто уже забрал внимание» пока держится на геометрии арен.

  - **Ворота A: PASS, но эталонный набор «защита мага» просел 50% → 30%** (на
    том же коммите базовой линии; остальные четыре в пределах ±2пп, дивергенция
    100%). Разобрано числами, не гипотезой: урон почти не перераспределился
    (Гром 3811 → 3964, Дарт 2477 → 2517, Лия 1423 → 1865 за 50 боёв), зато
    выросли смерти защитника (35 → 43) и следом подопечной (26 → 36). Заслон
    работает как задумано — враги идут в Грома, — но **односторонняя нянька
    концентрирует на защитнике весь урон**, а у Грома в этом наборе нет ничего,
    кроме «отгоняй ближайшего»: он ловит удары и падает, а с ним падает и Лия
    (третий в наборе, хрупкий Дарт, гибнет во всех 50 боях и до, и после).
    Это тот же урок, что дал план defense («дисциплина двусторонняя»), но по
    новой причине, и знак у него правильный: двусторонняя связка стала
    заметно сильнее (смерти мага против засады 20/20 → 4/20), односторонняя —
    слабее. Набор Ворот A не правил: он эталон, а не цель оптимизации.

  Следующий шаг: дочитать сокращённый перемер words-audit по всей базе слов
  (идёт в фоне, сравнение с базовой линией на HEAD) и закрыть план.
