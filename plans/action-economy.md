# План: Бой — экономика действий (3 AP)

Статус: в работе · команда: `continue action-economy plan`

## Проблематика

Ход юнита сейчас — одно решение: «перейти на клетку X и сделать одно из
{атака, защита, пас}» ([src/scoring.ts](../src/scoring.ts) `decide`). Движение
бесплатно и вшито в действие, поэтому у героя нет вопроса «на что потратить
ход» — есть только «куда встать и кого ударить». Это обедняет и бой, и главное —
язык принципов: словам не о чем спорить, когда выбор всего один.

Экономика на 3 очках действия (AP, по мотивам pf2e) даёт словам предмет:
«бей часто» против «бей наверняка», «прикройся» против «дожми», «прикрой
собой». Тот же ход становится разменом, и характер (линза) начинает читаться
по тому, **на что персонаж тратит очки**, а не только куда идёт.

Трудности:
- скоринг оценивает конечное состояние хода и не знает цены действий;
- вся балансировка (`tuning.ts`, статы героев и врагов, длина боя) исходит из
  «не больше одной атаки за раунд» — числа обнуляются;
- без MAP из pf2e ничто не мешает спамить дешёвую атаку — сдерживает только
  нелинейность урона (см. Проработка).

Смежные планы: `classes` (активки — та же экономика, делать после этого плана),
`aoe` и `foes` (тот же `scoring.ts`), `words` (новые концепты словаря),
`lenses` (линзы искажают выбор действий — здесь это главный выразительный рычаг).

## Проработка

### Набор действий

3 AP на ход. Действие можно повторять, пока хватает очков.

| Действие | AP | Эффект |
|---|---|---|
| шаг (`move`) | 1 | до `move` клеток (быстрые 3, медленные 1) |
| слабый удар (`weakAttack`) | 1 | `WEAK_ATK_MULT` × урона |
| удар (`attack`) | 2 | базовый урон |
| отчаянный удар (`selflessAttack`) | 2 | ×1.5 урона; входящий урон по себе ×1.5 до своего следующего хода |
| прикрыться (`cover`) | 1 | −25% входящего урона до своего следующего хода |
| глухая защита (`fullCover`) | 3 | −67% входящего урона |
| прикрыть союзника (`shieldAlly`) | 2 | союзнику эффект `cover` (−25%) до **его** следующего хода |
| пас (`wait`) | остаток | ничего, ход заканчивается |

Решения по числам, которых не было в исходной постановке:

- **`WEAK_ATK_MULT = 0.4`** (не 0.5). При линейном 0.5 три слабых удара дают
  1.5 базового против 1.0 у сильного — слабый удар доминировал бы всегда.
  При 0.4 три слабых = 1.2 урона против «удар + свободное AP на шаг или
  прикрытие»: обмен урона на позицию и защиту, а не строго лучший вариант.
  Это единственное, что до появления MAP сдерживает спам дешёвой атаки —
  трогать осознанно.
- **`cover` = 1 AP** (в одной группе с шагом и слабым ударом),
  `shieldAlly` = 2 AP, `fullCover` = 3 AP.
- **`fullCover` намеренно невыгоден по AP**: −67% за 3 AP против −25% за 1 AP —
  это 2.7× митигации за 3× цены. Он и не должен быть оптимальным: это ход
  «я сегодня не воюю», и тянуть к нему должны линзы (трус, параноик), а не
  математика.
- Длительность эффектов — «до своего следующего хода», как нынешний `defending`:
  ставится действием, снимается в начале своего хода. Исключение —
  `shieldAlly`: снимается в начале хода **прикрытого**.
- Урон: `dmg × (1 − coverLevel) × (exposed ? 1.5 : 1)`. Уровни прикрытия не
  складываются — берётся максимальный.

### Движение

`move` меняет смысл: было «клеток за раунд», стало «клеток за один шаг».
Значения пересчитываются в диапазон 1–3 (сейчас 2–4 у героев, 2–4 у врагов).
Потолок хода — до 3 шагов, то есть 3–9 клеток: поле 12×12 становится
проходимым за ход, и позиционные правила («держи дистанцию», «во фланг»)
получают реальную свободу вместо мелкой подстройки.

### Алгоритм решения (v1 — жадный)

Полный перебор планов хода (последовательности из 3 действий) отложен:
дорого и не нужно, пока не видно, что жадность врёт. v1:

```
ap = 3
пока ap > 0:
  кандидаты = действия, доступные при остатке ap (из текущей позиции)
  оценка = сумма факторов, как сейчас, делённая на цену действия
  выполнить лучшее, ap -= цена
  ('wait' завершает ход)
```

Каждое действие теперь отдельный кандидат: шаг — это `{kind:'move', to}`,
атаки считаются от текущей клетки. Позиционные `Preference` (`attack`-тяга,
`standoff`, `flank`, `behind`…) оценивают шаг ровно так же, как сейчас
оценивают клетку, — слой предпочтений не переписывается.

Известная слепота жадности: она не видит связку «два шага, потом сильный удар»,
если первый шаг сам по себе не выигрывает по очкам. Тяга к цели (`-0.6 × gap`)
делает шаг к врагу выгодным сам по себе, поэтому типовой случай проходит.
Фиксируем как принятый долг; если в логах пойдут ходы вида «шаг + прикрылся»
там, где напрашивался удар, — возвращаемся к перебору планов.

### Характер выбирает действие

Вместо отдельной системы «перков» — расширение `Instincts` полем
`actionBias: Partial<Record<ActionKind, number>>`. Это оставляет характер в
одном месте (линза) и делает новые действия искажаемыми по умолчанию.

Тяга входит в скоринг **слагаемым** `(тяга − 1) × ACTION_BIAS_WEIGHT`, а не
множителем на инстинкт. Множитель пробовали первым — вышло ровно наоборот
нужного: у прикрытия своего слагаемого в скоринге почти нет, и трус с ×2.5
уходил в глухую защиту в 98% ходов; а выбор между слабым и обычным ударом
перебивается премией правила, и там множителя не было видно вовсе (дуэлянт,
горячка и параноик давали ту же раскладку, что и обычный боец). Вкус характера
не должен зависеть от того, насколько велики прочие слагаемые в этой позиции.

Тяга 0 — не «редко», а запрет: действие не попадает в кандидаты вовсе.

| Линза | Смещение | Что видно в бою (тот же приказ «бей ближайшего») |
|---|---|---|
| обычный | — | удар 46%, слабый 43% |
| трус | `cover` ×1.5, `fullCover` ×1.2, `selflessAttack` ×0.2 | прикрывается 6%, отчаянно не бьёт |
| фанатик | `selflessAttack` ×2.5, `cover` ×0.3, `fullCover` ×0 | отчаянный удар 50%, не прикрывается вовсе |
| наседка | `shieldAlly` ×3 | закрывает союзника 10% (когда есть кого) |
| параноик | `cover` ×1.8 | нюанс: с активным приказом атаковать почти не виден |
| горячка | `weakAttack` ×3, `cover` ×0.5 | слабый удар 92% — три быстрых замаха вместо одного |
| дуэлянт | `weakAttack` ×0.7 | слабый удар 9% (было 43%) — бьёт в полную силу |
| позёр | `selflessAttack` ×1.5 | — |

Числа тяги подобраны по раскладке действий, а не назначены: у слабого удара
порог около ×2.75 — ниже него премия правила всё равно выводит вперёд удар за
2 очка, и характер не виден совсем; выше — боец переключается на три замаха.

### Слова

Действия должны быть выразимы принципами, иначе это невидимая игроку
внутрянка. Из пяти кандидатов в словарь пошли **три — манера удара**:
`act.strikeOften` («бить часто»), `act.strikeHard` («бить наверняка»),
`act.strikeDesperate` («бить отчаянно»). Они говорят, ЧЕМ бить; кого бить —
по-прежнему отдельное правило `attack`, так что манера комбинируется с любым
селектором.

«Прикройся» и «прикрой собой» НЕ добавлены: после шага 1 это уже существующие
слова — `act.brace` (глухая оборона) и `act.protect`/`act.coverRetreat` теперь
тратят очки хода на реальные действия прикрытия. Дубли — ровно то, что режет
план `words`.

Скоринг: премия своему виду атаки, **штраф чужому** (±`STRIKE_STYLE_BONUS`).
Штраф обязателен: без него «бей наверняка» не запрещал бы добирать слабым
ударом остаток очков, и слово читалось бы вполсилы.

Линзы искажают манеру: трус «отчаянно» → «наверняка», фанатик любую манеру →
«отчаянно», горячка «наверняка» → «часто». Все искажения с пометкой в source →
видны в карточке понимания.

Раскладка: пара часто/наверняка — в `CORE_WORDS` (простой вход в экономику),
отчаянно — в `DEEP_WORDS` (размен «сильнее бью — больнее получаю» стоит
понимать до того, как взял).

## Шаги

1. **Механика**: AP-цикл, набор действий, эффекты прикрытия/уязвимости
   (`types.ts`, `battle.ts`, `scoring.ts`, `tuning.ts`).
   Проверка: `battle.test` обновлён и зелёный, лог детерминирован байт-в-байт.
2. **Линзы**: `actionBias` в `Instincts` + таблица выше.
   Проверка: `lens.test` — трус чаще прикрывается, фанатик чаще бьёт отчаянно.
3. **Перебаланс**: `move` 1–3, `DMG_SCALE`, HP.
   Проверка: `balanceSweep`, типовой бой 10–14 раундов, Ворота A держатся.
4. **Слова**: концепты новых действий.
   Проверка: `constructor`/`cards`/`compiler` тесты.
5. **UI**: рендер новых событий и AP в разборе боя.

## Журнал

- 2026-08-05 — создан план сразу в проработке: набор действий и цены заданы
  пользователем, числа `WEAK_ATK_MULT`, цена `cover`, жадный алгоритм и
  `actionBias` — решения этой сессии (см. Проработка). MAP из pf2e осознанно
  отложен. Следующий шаг: 1 (механика).
- 2026-08-05 — **шаг 1 сделан**, все 188 тестов зелёные. По ходу вскрылись
  четыре вещи, которых в проработке не было:
  - **Пас должен списывать весь остаток очков.** Пока `wait` стоил ноль, а
    любое действие — очки, «постоять» обыгрывало всё: 88% ходов заканчивались
    ничем на полных 3 AP. Пас завершает ход, значит несделанное пропадает —
    это и есть его цена.
  - **Премии правил за атаку нормированы на цену действия** (`apShare`).
    Решение принимается по одному действию, поэтому плоская премия платилась
    за каждый удар — выгоднее всего было спамить слабый (33% всех действий
    против 2% обычного). После нормировки слабый и обычный удары идут 18/23%.
  - **Шаг, из которого цель простреливается, получает половину премии атаки.**
    Разделив шаг и удар, мы потеряли связь «встать так, чтобы бить именно X»:
    гладкая тяга `-0.6 × gap` почти не различает соседние цели.
  - **`move` пересчитан на 1–3 сразу** (пункт шага 3): при старых 2–4 юнит
    тратил 3 очка на бег и пересекал поле 12×12 за один ход, обнуляя позиционку.
  - Числа после шага 1 (сценарий Ворот A, 5 наборов × 20 сидов): раундов 9.0
    (цель 10–14), винрейт партии 0.80 (цель ~0.5) — перебаланс шага 3 нужен.
  - **Долг:** критерий 2 Ворот A даёт 0.60 на тестовой выборке в 20 сидов
    (порог), но на 200 сидах — 0.56 против 0.665 до изменений. Тест зелёный за
    счёт выборки, а не за счёт запаса. Гипотеза: бои стали вдвое короче (9
    раундов против 19), точек расхождения меньше. Перемерить после шага 3 и,
    если не вернётся, искать причину дальше — порог не трогать.

  Следующий шаг: 2 (`actionBias` у линз) — сейчас Гром-фанатик прикрывается в
  38% действий, что прямо противоречит характеру.
- 2026-08-05 — **шаг 2 сделан**, 192 теста зелёные (добавлены 4 на выбор
  действия характером). Тяга переехала с множителя на слагаемое — почему,
  расписано в «Проработке». Что всплыло сверх плана:
  - **`{ ...BASE }` — мелкая копия**, и `actionBias` уходил по ссылке: каждое
    применение линзы навсегда дописывало свою тягу в глобальный BASE. Ловилось
    тем, что три разные линзы давали одну и ту же раскладку действий.
  - **Премия правила «прикрывай X» за щит теперь масштабируется риском
    подопечного** (`shieldNeed`, полная премия при угрозе ≥30% его hp). Раньше
    платилась безусловно, и Гром работал телохранителем вместо бойца: набор
    guard-mage падал до 0.15 винрейта.
  - **Тяга труса к глухой защите оказалась двойным счётом**: у линзы уже есть
    `survival ×2.2`, который сам поднимает ценность прикрытия. С ×2.5 Лия
    уходила в fullCover в 32% ходов и партия теряла треть урона — винрейт всего
    сценария 0.56 → 0.23. Сбавили до ×1.2/×1.5: Лия прикрывается заметно чаще
    обычного (16% против 5%), но не выключается из боя.
  - Числа после шага 2: раундов 12.5, винрейт партии 0.43 (был 0.56 — характеры
    стоят партии силы, потому что у врагов линзы почти всегда `plain`),
    критерий метки 0.188 запаса при пороге 0.15.
  - Выпадает набор `turtle` — 0.10 винрейта против 0.78 на шаге 1: «держи
    позицию» + трус = стоят и прикрываются. Разобрать в шаге 3.

  Следующий шаг: 3 (перебаланс).
- 2026-08-05 — **шаг 3 сделан**, 192 теста зелёные. Кривая забега вернулась к
  доэкономическим значениям: забег выигран 13% (было 13%), босс достигнут 27%
  (28%), урок 47–52% (52%), раундов 12.8. Правились две конструкторские ошибки,
  а не числа:
  - **У слабого удара не было ни одной сильной стороны.** При `WEAK_ATK_MULT`
    0.4 три слабых удара дают 1.2 базового против 1.4 у связки «удар + слабый»,
    то есть волюм строго хуже по всем осям. Характер, тянущий к частым ударам,
    оказывался чистым штрафом: горячка теряла 27 очков винрейта на уроке.
    0.45 (1.35 против 1.45) оставляет волюм слегка слабее, но играбельным.
  - **Отчаянный удар при 1.5/1.5 убыточен по устройству**: бьёшь одного,
    получаешь ×1.5 от всех четверых. Фанатик, которого к нему тянет, терял
    24 очка. Уязвимость снижена до 1.35 — при 1.25 удар становится лучшим за
    очко в игре, его жмут все подряд, и характер снова не читается.
  - Премия правила за щит союзнику вынесена в `SHIELD_RULE_BONUS` и снижена
    2.5 → 1.4: прикрыть — часть приказа «прикрывай X», но не замена бою.
  - Разброс силы линз на уроке: 32% (трус) … 73% (славолюб) против 20% … 70%
    до экономики — диапазон тот же, крайние поменялись местами. Наседка (44%)
    просела: её врождённое правило «прикрывать отход» теперь тратит очки хода,
    а не только позицию.

  **Открытый долг — критерий 1 Ворот A.** Разброс винрейтов между наборами
  принципов: 0.400 на тестовой выборке в 20 сидов (порог 0.3, тест зелёный), но
  0.17–0.205 на 100–200 сидах против устойчивых 0.375–0.400 до экономики. Тест
  проходит за счёт выборки. Причина содержательная: три очка хода снизили цену
  позиционных принципов — раньше «держи позицию» означало отказ от сближения, а
  теперь можно и стоять, и бить дважды, поэтому стратегии сходятся по исходу.
  Критерий 2 (расхождение процесса), наоборот, вырос: 0.77 против 0.665.

  Пробовали чинить через `AP_VALUE` (цена очка хода): 1.5 даёт разброс 0.30,
  2.0 — 0.38, то есть до уровня базы. Цена — трус: 32% → 14% → 9% побед на
  уроке; поднимать ему тягу к прикрытию бесполезно, становится только хуже
  (прикрытие не спасает, а тратит ход). Это размен «критерий 1 против
  разнообразия характеров», решать не тюнингом.

  **Решение: отложить до шага 4, `AP_VALUE` оставить 0.5.** Пять наборов
  принципов в [scenarios.ts](../src/scenarios.ts) написаны под старую экономику:
  в них нет ни прикрытий, ни выбора вида удара — то есть мера меряет стратегии,
  которые новую экономику не используют. Шаг 4 открывает эти действия словам;
  после него переписать наборы и перемерить разброс. Если сам не вернётся —
  возвращаться к размену с `AP_VALUE` или переформулировать критерий.

  Следующий шаг: 4 (слова), с перемером критерия 1 в конце.
- 2026-08-05 — **шаг 4 сделан**, 204 теста зелёные (новый
  `strike-style.test.ts`, 12 тестов). Три слова манеры удара во всех слоях:
  vocab → ir → scoring → constructor → cards → схема/промпт компилятора
  (`PROMPT_VERSION` 4 → 5, кэш инвалидирован; заодно поправлена спека
  `act.brace` — обещала «урон вдвое», что неправда с шага 1). Детали дизайна —
  в секции «Проработка. Слова».
  - **Ставка «разброс вернётся сам» сыграла.** Наборы Ворот A переписаны на
    новые слова (rush бьёт отчаянно, turtle — наверняка), и критерий 1 вырос
    с 0.205 до **0.475–0.55 на любой выборке** — выше доэкономических 0.375.
    Разброс содержательный: манера удара имеет цену (rush 0.45 — отчаянность
    подставляет, turtle 0.30 — отказ от добивания слабым, focus-leader 0.82).
    `AP_VALUE` трогать не пришлось, характеры целы.
  - `makeRushVariant` при этом начал стирать и правило манеры (мерял смену
    двух правил, дивергенция взлетала до 1.000) — поправлен: подменяет ровно
    правило атаки, дивергенция 0.845.
  - Кривая забега не сдвинулась: 12% выигрышей, 28% босса (было 13/27).

  Следующий шаг: 5 (UI) — рендер новых событий и AP уже есть с шага 1,
  осталось показать манеру удара в конструкторе чипсов.
